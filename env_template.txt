# Environment Variables Template
# Copy this to .env in the project root and fill in your values.
#
#   cp env_template.txt .env

# ── Google Gemini (required) ──────────────────────────────────────────────
GEMINI_API_KEY=your-gemini-api-key-here

# ── Supabase (required for auth + history) ───────────────────────────────
# Project URL — found in Supabase dashboard → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co

# Anon/public key — safe to use client-side (row-level security enforced)
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Service role key — ONLY used server-side in fastapi_app.py, NEVER in Streamlit
SUPABASE_SERVICE_KEY=your-supabase-service-role-key-here

# ── API URLs (optional overrides) ────────────────────────────────────────
API_BASE_URL=http://localhost:8000
API_HOST=0.0.0.0
API_PORT=8000
