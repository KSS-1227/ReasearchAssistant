apiVersion: v1
kind: Service
metadata:
  name: research-assistant-api
  labels:
    app: research-assistant
    tier: api
spec:
  type: NodePort
  selector:
    app: research-assistant
  ports:
    - name: api
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30081
