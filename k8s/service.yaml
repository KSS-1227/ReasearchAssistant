apiVersion: v1
kind: Service
metadata:
  name: research-assistant-service
  labels:
    app: research-assistant
spec:
  type: NodePort
  selector:
    app: research-assistant
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
      nodePort: 30080